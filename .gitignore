# OS
.DS_Store

# Secrets — never commit (.env.example is committed; .env is not)
.env
.env.*
!.env.example

# Node / build (forward-looking for Task 1+)
node_modules/
.next/
dist/
build/
coverage/
*.log

# Backups — encrypted dumps live off-box, never in the repo
*.dump.enc
comop-offsite/
